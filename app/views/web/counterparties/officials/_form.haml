= simple_form_for [resource_counterparty, official] do |f|
  = f.input :name
  = f.input :position
  = f.input :contacts, as: :big_textarea
  = f.association :counterparty, as: :hidden
  = f.button :submit